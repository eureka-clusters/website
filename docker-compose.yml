version: '3.9'

services:
  reverse-proxy:
    # The official v2 Traefik docker image
    image: traefik:v2.5
    container_name: reverse-proxy
    # Enables the web UI and tells Traefik to listen to docker
    command:
      - --log.level=INFO
      - --providers.docker
      - --providers.docker.network=web
      - --certificatesresolvers.letsencrypt.acme.httpchallenge=true
      - --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=http
      - --certificatesresolvers.letsencrypt.acme.email=johan.van.der.heide@itea4.org
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --entrypoints.http.address=:80
      - --entrypoints.http.http.redirections.entrypoint.to=https
      - --entrypoints.http.http.redirections.entrypoint.scheme=https
      - --entrypoints.https.address=:443
      - --entrypoints.https.http.tls=true
      - --entrypoints.https.http.tls.certResolver=letsencrypt
      - --entrypoints.https.http.tls.domains[0].main=eurekaclusters.eu
      - --entrypoints.https.http.tls.domains[1].main=api.eurekaclusters.eu
    networks:
      web: { }
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # So that Traefik can listen to the Docker events
      - /var/run/docker.sock:/var/run/docker.sock
      - certs:/letsencrypt

  api-webserver:
    image: 'nginx:alpine'
    container_name: api-webserver
    volumes:
      - './:/var/www'
      - './.docker/nginx:/etc/nginx/conf.d'
    labels:
      - traefik.enable=true
      - traefik.http.services.api-webserver.loadbalancer.server.port=80
      - traefik.http.routers.api-webserver.service=api-webserver
      - traefik.http.routers.api-webserver.rule=Host(`api.eurekaclusters.eu`)
      - traefik.http.routers.api-webserver.entrypoints=https
      - traefik.http.routers.api-webserver.tls=true
    networks:
      - internal
      - web

  api-app:
    image: ghcr.io/eureka-clusters/backend:latest
    container_name: api-app
    labels:
      - traefik.enable=false
    volumes:
      - ./conf/doctrine_orm.global.php:/var/www/config/autoload/doctrine_orm.global.php
      - ./conf/oauth2.services.global.php:/var/www/config/autoload/oauth2.services.global.php
    networks:
      - internal
      - web

  portal:
    image: ghcr.io/eureka-clusters/backend/production:latest
    container_name: portal
    labels:
      - traefik.enable=true
      - traefik.http.services.portal.loadbalancer.server.port=80
      - traefik.http.routers.portal-https.service=portal
      - traefik.http.routers.portal-https.rule=Host(`eurekaclusters.eu`)
      - traefik.http.routers.portal-https.entrypoints=https
      - traefik.http.routers.portal-https.tls=true
    networks:
      - web

volumes:
  certs:

networks:
  web:
    external: true
  internal:
    external: false